#import "/src/book.typ": *

#show : book-page.with(title: "Complemento")

= Complemento

En teoría de #cross-link("/content/240724120916.typ")[conjuntos] el complemento de un conjunto $A$, denotado como $A', A^c$, o $not A$, se refiere al conjunto de todos los elementos que no están en $A$, pero son elementos de un conjunto universal $U$ que se ha definido previamente. Matemáticamente, se define como:

$ A' = x in U : x in.not A $

Aquí, $U$ es el conjunto universal que contiene todos los objetos bajo consideración, y $A'$ es el complemento de $A$ respecto a $U$.
